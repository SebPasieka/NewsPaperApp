describe('<ArticleList/>',  () => {

    it('should render a list of items recieved via props', () => {})

    it('should render -nothing found- when list of items recieved via props is empty', () => {})

});